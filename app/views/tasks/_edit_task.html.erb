<%= form_for [task.project, task], url: project_task_path(task.project, task), :html => { :id => "form_edit_task_#{task.id}", :class => "form_edit_task"} do |f| %>
            <%= f.label :name %><br>
            <%= f.text_field :name %>
            <%= f.submit "Update task" %>
<% end %>